---
# tasks file for os-bootstrap
#- name: upgrade all packages
#  yum: 
#    name: "*" 
#    state: latest
# - name: install epel-release
#   yum:
#     name: "epel-release"
#     update_cache: true
- name: install htop
  apt:
    name: "htop"
    state: latest
    update_cache: true
- name: install sysstat
  apt:
    name: "sysstat"
    state: latest
    update_cache: true